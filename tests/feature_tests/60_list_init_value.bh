# Test 60: List[int] initialized with non-empty value like [0]
try:
    from typing import List
except:
    pass

class Stack:
    def __init__(self):
        self.items: List[int] = [0]

    def get_count(self) -> int:
        return len(self.items)

    def top(self) -> int:
        n: int = len(self.items)
        if n > 0:
            return self.items[n - 1]
        return -1

def main() -> int:
    print("List init value test\n")

    s: Stack = Stack()

    # Check initial count
    n: int = s.get_count()
    if n == 1:
        print("Count: OK\n")
    else:
        print("Count: FAIL\n")

    # Check initial value
    v: int = s.top()
    if v == 0:
        print("Value: OK\n")
    else:
        print("Value: FAIL\n")

    print("Done\n")
    return 0
