# Minimal: compound or condition with default method call
class Foo:
    def __init__(self):
        self.val: int = 0

    def make(self, x: int = 5) -> int:
        return x

    def run(self, ch: char):
        if ch == 'a' or ch == 'b':
            result: int = self.make()
            self.val = result

def main() -> int:
    f: Foo = Foo()
    f.run('a')
    if f.val == 5:
        print("OK\n")
    else:
        print("FAIL\n")
    return 0
