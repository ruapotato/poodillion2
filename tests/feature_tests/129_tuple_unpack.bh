# Test tuple unpacking assignment (like start_line, start_col = self.line, self.column)
def main() -> int:
    a: int = 0
    b: int = 0
    a, b = 10, 20
    if a == 10 and b == 20:
        print("OK\n")
    else:
        print("FAIL\n")
    return 0
