# Test assigning single-quote string to char variable
def main() -> int:
    ch: char = 'x'
    ch = "'"
    if ord(ch) == 39:
        print("OK\n")
    else:
        print("FAIL: ")
        c: char = chr(48 + ord(ch))
        print(c)
        print("\n")
    return 0
