# Test while loop with method call condition (like while not self.at_end())
class Counter:
    value: int
    max_val: int

    def init(self, max_v: int) -> Counter:
        self.value = 0
        self.max_val = max_v
        return self

    def at_end(self) -> int:
        if self.value >= self.max_val:
            return 1
        return 0

    def advance(self) -> void:
        self.value = self.value + 1

def main() -> int:
    c: Counter = Counter().init(3)
    count: int = 0
    while c.at_end() == 0:
        count = count + 1
        c.advance()
    if count == 3:
        print("OK\n")
    else:
        print("FAIL\n")
    return 0
