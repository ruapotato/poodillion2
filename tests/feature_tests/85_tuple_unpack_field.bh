# Test tuple unpacking with self.field access
class Foo:
    def __init__(self):
        self.line: int = 5
        self.column: int = 3

    def unpack_test(self) -> int:
        start_line, start_col = self.line, self.column
        return start_line + start_col

def main() -> int:
    f: Foo = Foo()
    v: int = f.unpack_test()
    if v == 8:
        print("OK\n")
    else:
        print("FAIL\n")
    return 0
