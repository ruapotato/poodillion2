# Test 41: Nested field access - using pointer field
class Inner:
    value: int32

class Outer:
    x: int32
    y: int32

def main():
    outer: Outer = Outer(10, 20)

    x: int32 = outer.x
    if x == 10:
        print("OK1\n")

    y: int32 = outer.y
    if y == 20:
        print("OK2\n")
