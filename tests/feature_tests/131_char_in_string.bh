# Test checking if char is in a string (like ch in '0123456789')
def is_digit(ch: char) -> int:
    digits: str = "0123456789"
    i: int = 0
    while i < 10:
        if ord(ch) == ord(digits[i]):
            return 1
        i = i + 1
    return 0

def main() -> int:
    if is_digit('5') == 1 and is_digit('a') == 0:
        print("OK\n")
    else:
        print("FAIL\n")
    return 0
