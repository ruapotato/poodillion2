# Test 26: Just alloc
def main():
    print("Before alloc\n")

    # Manually call alloc via inline asm
    # This is to test if alloc works at all
    ptr: int32 = 0
    asm("push 16")
    asm("call alloc")
    asm("add esp, 4")
    asm("mov [ebp-4], eax")

    print("After alloc\n")
