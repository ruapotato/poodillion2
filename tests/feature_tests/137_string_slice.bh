# Test string slicing (like self.source[start:end])
def main() -> int:
    s: str = "hello world"
    sub: str = s[0:5]
    if sub == "hello":
        print("OK\n")
    else:
        print("FAIL: ")
        print(sub)
        print("\n")
    return 0
