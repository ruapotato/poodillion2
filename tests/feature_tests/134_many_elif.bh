# Test many elif branches (like keyword lookup)
def lookup(s: str) -> int:
    if s == "if":
        return 1
    elif s == "else":
        return 2
    elif s == "elif":
        return 3
    elif s == "while":
        return 4
    elif s == "for":
        return 5
    elif s == "def":
        return 6
    elif s == "class":
        return 7
    elif s == "return":
        return 8
    else:
        return 0

def main() -> int:
    r1: int = lookup("if")
    r2: int = lookup("while")
    r3: int = lookup("unknown")
    if r1 == 1 and r2 == 4 and r3 == 0:
        print("OK\n")
    else:
        print("FAIL\n")
    return 0
