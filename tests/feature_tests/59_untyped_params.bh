# Test 59: Untyped parameters (like Token class in lexer.py)
class Token:
    def __init__(self, ttype, value, line: int):
        self.ttype = ttype
        self.value = value
        self.line: int = line

def main() -> int:
    print("Untyped params test\n")

    t: Token = Token(1, "hello", 10)

    if t.line == 10:
        print("Line: OK\n")
    else:
        print("Line: FAIL\n")

    print("Done\n")
    return 0
