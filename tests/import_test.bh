# Test import functionality
import "lib/math"

proc main(): int32 =
    var a: int32 = 5
    var b: int32 = 7

    # Use imported functions
    var sum: int32 = add(a, b)      # Should be 12
    var prod: int32 = multiply(a, b) # Should be 35
    var sq: int32 = square(sum)     # Should be 144

    # Return something to verify: 12 + 35 = 47
    return add(sum, prod)
