#!/usr/bin/pooscript
# login - User authentication for Poodillion
# Authenticates users and spawns their shell

# For now, this is a simplified version
# In the real game, this would check /etc/passwd and /etc/shadow

print("Poodillion login: ", end='')

# Since we can't do interactive input in PooScript yet,
# this is just a placeholder that shows the concept

# In the full implementation:
# 1. Prompt for username
# 2. Prompt for password (hidden)
# 3. Check /etc/shadow
# 4. Set UID/GID
# 5. chdir to home
# 6. exec pooshell

error("login: Interactive mode not yet implemented")
error("login: Use direct shell spawn for now")
exit(1)
