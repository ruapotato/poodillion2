#!/usr/bin/pooscript
# mkdir - create directories

if len(args) == 0:
    error("mkdir: missing operand")
    exit(1)

for path in args:
    try:
        vfs.mkdir(path, 0o755)
    except RuntimeError as e:
        error(f"mkdir: cannot create directory '{path}'")
        exit(1)

exit(0)
